name: Incident Response

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

jobs:
  respond:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run incident response workflow
        run: antigravity run incident-response
