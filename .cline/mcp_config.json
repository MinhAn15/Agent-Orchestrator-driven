{
  "mcpServers": {
    "antigravity": {
      "command": "python",
      "args": ["-m", "antigravity.cli", "mcp", "--stdio"],
      "env": {
        "LLM_PROVIDER": "${LLM_PROVIDER}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
